<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h2>🎉 New User Signup on PlanMyDay!</h2>
    
    <p><strong>Name:</strong> <%= @user.first_name %> <%= @user.last_name %></p>
    <p><strong>Email:</strong> <%= @user.email_address %></p>
    <p><strong>User ID:</strong> <%= @user.id %></p>
    <p><strong>Signed up:</strong> <%= @signup_time %></p>
    
    <hr style="margin: 20px 0;">
    
    <h3>User Stats:</h3>
    <ul>
      <li><strong>Total Users:</strong> <%= User.count %></li>
      <li><strong>Users Today:</strong> <%= User.where('created_at >= ?', Date.current.beginning_of_day).count %></li>
      <li><strong>Users This Week:</strong> <%= User.where('created_at >= ?', 1.week.ago).count %></li>
      <li><strong>Users This Month:</strong> <%= User.where('created_at >= ?', 1.month.ago).count %></li>
    </ul>
    
    <hr style="margin: 20px 0;">
    
    <p>
      <small>
        This is an automated notification sent because a new user signed up for PlanMyDay.
      </small>
    </p>
  </body>
</html>